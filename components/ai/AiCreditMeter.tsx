import { Card } from "@/components/ui/Card";
export function AiCreditMeter({ used = 7, limit = 25 }: { used?: number; limit?: number }) { const pct = Math.min(100, Math.round((used/limit)*100)); return <Card><div className="flex items-center justify-between"><div><h3 className="font-semibold">AI credits</h3><p className="text-sm text-slate-500">{limit-used} credits remaining this month</p></div><div className="text-sm font-bold">{used}/{limit}</div></div><div className="mt-3 h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-slate-950" style={{ width: `${pct}%` }} /></div></Card>; }
